<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.moneymanager">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/NoActionBarAppTheme">
        <activity android:name=".activities.AMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.accounts.AAccounts">
        </activity>
        <activity
                android:name=".activities.ASearch"
                android:parentActivityName=".activities.AMain">
        </activity>
        <activity android:name=".activities.ATransaction">
        </activity>
        <activity android:name=".activities.accounts.AAddAccount">
        </activity>
    </application>

</manifest>
